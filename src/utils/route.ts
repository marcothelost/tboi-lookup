/**
 * Base title for pages in the application.
 * @readonly
 */
export const PAGE_TITLE_BASE = 'TBOI Lookup';
/**
 * Separator used to split the base page title and specific page titles.
 * @readonly
 */
export const PAGE_TITLE_SPLITTER = ' | ';

/**
 * Enumeration representing different routes in the application.
 * @enum {string}
 * @readonly
 */
export enum AppRoute {
  HOME = '/',
  NOT_FOUND = '*',
}
